const Icon = ({social})=>{
    return (
    <span>
        <a href={social.fb}>fb</a>
        <a href={social.insta}>insta</a>
        <a href={social.linkedIn}>LinkedIn</a>
    </span>
    )
}
export default Icon